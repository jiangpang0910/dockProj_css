# Makes `python3 -m pipeline.cli` work from the repo root. The real package is dockparse/ (self-contained, so
# pipeline/ can also be deployed on its own as a Vercel project with Root Directory = pipeline/).
